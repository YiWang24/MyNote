import Note from "@/components/note/Note";
import SideBar from "@/components/note/NoteSideBar";
import React from "react";

const page = () => {
  return (
    <main className=" w-full h-full flex justify-center items-center">
      <Note />
    </main>
  );
};

export default page;
